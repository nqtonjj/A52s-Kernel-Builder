name: Build Kernel A52s SukiSU (Ultimate Hybrid)

on:
  workflow_dispatch:

jobs:
  build:
    # Pin phien ban Ubuntu 22.04 de on dinh moi truong Clang
    runs-on: ubuntu-22.04

    steps:
      - name: 1. Cai dat cong cu
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y bc build-essential git flex bison lz4 libssl-dev libelf-dev clang-14 lld-14 llvm-14 gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi binutils-aarch64-linux-gnu cpio python3 python3-pip zstd rsync zip curl

      - name: 2. Tai ma nguon (Nhanh oneui-ksu)
        run: |
          # Nhanh nay cua JingMatrix da co san manual hooks trong fs/
          git clone --depth=1 -b oneui-ksu https://github.com/JingMatrix/samsung_sm7325.git kernel

      # [Hoc tu Claude]: Tach rieng buoc Toolchain de de quan ly
      - name: 3. Lien ket Toolchain Clang 14
        run: |
          sudo ln -sf /usr/bin/clang-14        /usr/bin/clang
          sudo ln -sf /usr/bin/ld.lld-14       /usr/bin/ld.lld
          sudo ln -sf /usr/bin/llvm-objcopy-14 /usr/bin/llvm-objcopy
          sudo ln -sf /usr/bin/llvm-ar-14      /usr/bin/llvm-ar
          sudo ln -sf /usr/bin/llvm-nm-14      /usr/bin/llvm-nm
          sudo ln -sf /usr/bin/llvm-strip-14   /usr/bin/llvm-strip
          sudo ln -sf /usr/bin/llvm-objdump-14 /usr/bin/llvm-objdump
          sudo ln -sf /usr/bin/llvm-readelf-14 /usr/bin/llvm-readelf
          clang --version | head -1

      - name: 4. Tich hop SukiSU-Ultra (Theo tieu chuan SukiSU)
        run: |
          cd kernel
          
          # Don dep sa lon
          rm -rf drivers/kernelsu drivers/KernelSU drivers/KernelSU-Next KernelSU
          sed -i '/[kK]ernel[sS][uU]/d' drivers/Kconfig drivers/Makefile || true

          # [FIX ĐỘT PHÁ CỦA CHÚNG TA]: Chốt sổ (Commit) việc dọn dẹp để setup.sh KHÔNG THỂ undo qua lệnh git stash
          git config --global user.email "bot@github.com"
          git config --global user.name "GitHub Action"
          git commit -am "Xoa sach KSU cu cua JingMatrix de tranh xung dot" || true

          # Dung script setup.sh chinh thuc voi nhanh susfs-main
          echo "Dang chay SukiSU setup.sh..."
          curl -LSs "https://raw.githubusercontent.com/sukisu-ultra/sukisu-ultra/main/kernel/setup.sh" | bash -s susfs-main

          # [Hoc tu Claude]: Kiem tra cheo (Cross-check) de bat loi som
          echo "=== Kiem tra drivers/Kconfig ==="
          grep -i "kernelsu" drivers/Kconfig || { echo "LOI: KernelSU chua duoc link vao Kconfig!"; exit 1; }
          echo "=== Kiem tra drivers/Makefile ==="
          grep -i "kernelsu" drivers/Makefile || { echo "LOI: KernelSU chua duoc link vao Makefile!"; exit 1; }

      - name: 5. Patch Code & Fix Loi Compile
        run: |
          cd kernel

          # Fix chung cho pgtable.h cua KSU (An toan hon, tranh bao loi thu muc khong ton tai)
          for DIR in KernelSU drivers/kernelsu; do
            if [ -d "$DIR" ]; then
              find "$DIR" -type f \( -name "*.c" -o -name "*.h" \) -print0 | xargs -0 grep -l "linux/pgtable.h" 2>/dev/null | while IFS= read -r f; do
                sed -i 's|#include <linux/pgtable.h>|#include <linux/mm.h>|g' "$f"
              done
            fi
          done || true

          # [NUCLEAR FIX TỪ ẢNH LOG]: Biến mọi bình luận tài liệu (/**) thành bình thường (/*) 
          # Trình biên dịch sẽ tự động lờ đi và KHÔNG kiểm tra thiếu parameter 'q' nữa!
          echo "Patching xt_quota2.c (Vô hiệu hóa Doc Comment)..."
          if [ -f "net/netfilter/xt_quota2.c" ]; then
            sed -i 's/\/\*\*/\/\*/g' net/netfilter/xt_quota2.c || true
          fi

          # Xoa -Werror bi hardcode chìm trong mọi Makefile con của netfilter
          find net/netfilter -name "Makefile" -exec sed -i 's/-Werror//g' {} + || true

      - name: 6. Tien hanh nau Kernel (Config & Build)
        run: |
          cd kernel

          CONFIG_FILE=$(find arch/arm64/configs -name "*a52sxq*defconfig" | grep -v -e "kor" -e "chn" | head -n 1 | sed 's|arch/arm64/configs/||')
          echo "Dung config: $CONFIG_FILE"

          # Tiem cac CONFIG thiet yeu nhat cho SukiSU + Manual Hook
          echo "CONFIG_KSU=y" >> arch/arm64/configs/$CONFIG_FILE
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> arch/arm64/configs/$CONFIG_FILE
          echo "CONFIG_KPM=y" >> arch/arm64/configs/$CONFIG_FILE
          echo "CONFIG_KALLSYMS=y" >> arch/arm64/configs/$CONFIG_FILE
          echo "CONFIG_KALLSYMS_ALL=y" >> arch/arm64/configs/$CONFIG_FILE
          
          # Tiem SusFS configs vi ta dang dung nhanh susfs-main
          echo "CONFIG_KSU_SUSFS=y" >> arch/arm64/configs/$CONFIG_FILE
          echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> arch/arm64/configs/$CONFIG_FILE
          echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> arch/arm64/configs/$CONFIG_FILE
          echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> arch/arm64/configs/$CONFIG_FILE
          echo "CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y" >> arch/arm64/configs/$CONFIG_FILE
          echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> arch/arm64/configs/$CONFIG_FILE

          # [FIX QUAN TRONG TU LOG]: Dinh nghia args bien dich giong het nhau cho tat ca cac lenh make
          MAKE_ARGS="O=out ARCH=arm64 CC=clang CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- LLVM=1 LLVM_IAS=1 CLANG_TRIPLE=aarch64-linux-gnu-"

          # Truyen bien Clang ngay tu khi tao config de khong bi dung man hinh hoi (y/n)
          make $MAKE_ARGS $CONFIG_FILE
          yes "" | make $MAKE_ARGS olddefconfig

          # [Hoc tu Claude]: Xac nhan lai lan cuoi xem file config co nhan hay khong
          echo "=== Kiem tra CONFIG quan trong ==="
          for KEY in CONFIG_KSU CONFIG_KSU_MANUAL_HOOK CONFIG_KPM CONFIG_KALLSYMS CONFIG_KALLSYMS_ALL; do
            val=$(grep "^${KEY}=" out/.config 2>/dev/null || echo "(khong duoc bat)")
            echo "  $KEY = $val"
          done

          # [Hoc tu Claude]: Ghi Log thong minh
          echo ">> Bat dau build voi $(nproc --all) cores..."
          make -j$(nproc --all) $MAKE_ARGS \
               KBUILD_WERROR=0 \
               KCFLAGS="-Wno-error -Wno-align-mismatch -Wno-pointer-to-int-cast -Wno-incompatible-pointer-types -Wno-implicit-function-declaration" \
               W=0 2>&1 | tee ../build.log
          
          BUILD_EXIT=${PIPESTATUS[0]}
          if [ "$BUILD_EXIT" -ne 0 ]; then
            echo ""
            echo "══════════════════════════════════════════"
            echo "BUILD THAT BAI - 100 DONG LOG CUOI:"
            echo "══════════════════════════════════════════"
            tail -100 ../build.log
            exit 1
          fi

      - name: 7. Dong goi voi AnyKernel3 (Ho tro Un1ca ROM)
        if: success()
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git AnyKernel3
          cd AnyKernel3
          
          # Toi uu block cho AnyKernel
          sed -i 's/kernel.string=.*/kernel.string=SukiSU-Ultra A52s/g' anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
          sed -i 's|block=/dev/block/platform/omap/omap_hsmmc.0/by-name/boot;|block=auto;|g' anykernel.sh
          
          # [Hoc tu Claude]: Tu dong nhan dien duoi file mo rong
          cp ../kernel/out/arch/arm64/boot/Image .
          [ -f ../kernel/out/arch/arm64/boot/Image.gz ] && cp ../kernel/out/arch/arm64/boot/Image.gz .
          [ -f ../kernel/out/arch/arm64/boot/Image.gz-dtb ] && cp ../kernel/out/arch/arm64/boot/Image.gz-dtb .
          
          zip -r9 ../Kernel-A52s-SukiSU-Un1ca.zip * -x '*.git*' -x 'README.md'

      - name: 8. Upload thanh pham Flashable Zip
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-A52s-SukiSU-Un1ca-Flashable
          path: Kernel-A52s-SukiSU-Un1ca.zip

      # [Hoc tu Claude]: Upload log de de dang chan doan neu chet
      - name: 9. Upload Build Log (chi khi that bai)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-debug
          path: build.log
